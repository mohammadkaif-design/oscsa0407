#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_LINE 1024

int main(int argc, char *argv[]) {
    FILE *fp;
    char line[MAX_LINE];
    char *pattern;

    // Check arguments: ./a.out pattern filename
    if (argc != 3) {
        fprintf(stderr, "Usage: %s <pattern> <filename>\n", argv[0]);
        exit(1);
    }

    pattern = argv[1];

    // Open file
    fp = fopen(argv[2], "r");
    if (fp == NULL) {
        perror("fopen");
        exit(1);
    }

    // Read file line by line
    while (fgets(line, sizeof(line), fp)) {
        if (strstr(line, pattern)) {  // If pattern found in line
            printf("%s", line);
        }
    }

    fclose(fp);
    return 0;
}
